# skir_client

`skir_client` is the runtime library used by Skir-generated C# code.

## Install

```sh
dotnet add package skir_client
```

## Develop

```sh
dotnet build SkirClient.csproj
dotnet test SkirClientTest/SkirClientTest.csproj
```
